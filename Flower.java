import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)


public abstract class Flower extends Actor
{
    public abstract int getScore();
    /**
     * Act - do whatever the Flower wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        //Check is eats Ladybug
        //boolean contactLadybug = isTouching(Ladybug.class);
        //System.out.println(contactLadybug);
        //if(contactLadybug){
        //   removeTouching(Ladybug.class);
            
      //  }
    }    
}
